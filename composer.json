{
    "require": {
        "persiliao/git-webhooks": "^0.1.0"
    },
    "scripts": {
        "post-root-package-install": [
            "@php -r \"file_exists('.config.example.php') || copy('.config.example.php', 'config.php');\""
        ]
    }
}
