{
    "require": {
        "persiliao/git-webhooks": "^0.1.0"
    },
    "scripts": {
        "post-update-cmd": [
            "@php -r \"file_exists('config.php') || copy('.config.example.php', 'config.php');\""
        ]
    }
}
